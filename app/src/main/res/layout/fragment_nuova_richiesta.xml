<?xml version="1.0" encoding="utf-8"?>
<!-- Il tag <layout> è obbligatorio per generare FragmentNuovaRichiestaBinding
     (vedi bug corretto in fragment_splash.xml: senza, DataBinding fallisce
     silenziosamente a runtime invece che a compile-time). -->
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- NestedScrollView: rende la schermata scrollabile, così quando la
         tastiera è aperta il campo attivo può salire sopra di essa. -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/care_background"
        android:clipToPadding="false"
        android:fillViewport="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/screen_padding">

            <!-- Titolo in pagina rimosso: lo mostra la Toolbar, che in modifica
                 diventa "Modifica richiesta" (gestito nel Fragment). -->

            <!-- Tipo di aiuto: set chiuso di opzioni, come richiesto. "Altro" è
                 sempre selezionabile e va bene anche senza compilare il campo
                 sotto: in quel caso il tipo salvato resta la stringa "altro". -->
            <RadioGroup
                android:id="@+id/tipoRadioGroup"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <RadioButton
                    android:id="@+id/tipoSpesaRadio"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="@dimen/touch_target_min"
                    android:text="Spesa"
                    android:textSize="@dimen/text_size_body" />

                <RadioButton
                    android:id="@+id/tipoBollettaRadio"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="@dimen/touch_target_min"
                    android:text="Bolletta"
                    android:textSize="@dimen/text_size_body" />

                <RadioButton
                    android:id="@+id/tipoAssistenzaDigitaleRadio"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="@dimen/touch_target_min"
                    android:text="Assistenza digitale"
                    android:textSize="@dimen/text_size_body" />

                <RadioButton
                    android:id="@+id/tipoAltroRadio"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="@dimen/touch_target_min"
                    android:text="Altro"
                    android:textSize="@dimen/text_size_body" />

            </RadioGroup>

            <!-- Visibile solo quando è selezionato "Altro" (gestito nel Fragment).
                 Se lasciato vuoto, il tipo salvato resta semplicemente "altro". -->
            <EditText
                android:id="@+id/altroInput"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_small"
                android:hint="Specifica (facoltativo)"
                android:textSize="@dimen/text_size_body"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@id/tipoRadioGroup"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <EditText
                android:id="@+id/descrizioneInput"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_large"
                android:hint="Descrivi di cosa hai bisogno"
                android:textSize="@dimen/text_size_body"
                android:minLines="3"
                android:gravity="top|start"
                app:layout_constraintTop_toBottomOf="@id/altroInput"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <TextView
                android:id="@+id/errorText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_small"
                android:textColor="@color/care_error"
                android:textSize="@dimen/text_size_body"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@id/descrizioneInput"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <Button
                android:id="@+id/inviaButton"
                android:layout_width="match_parent"
                android:layout_height="@dimen/button_height"
                android:layout_marginTop="@dimen/spacing_large"
                android:text="Invia richiesta"
                android:textSize="@dimen/text_size_button"
                android:backgroundTint="@color/care_primary"
                android:textColor="@color/care_on_primary"
                app:layout_constraintTop_toBottomOf="@id/errorText"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

            <ProgressBar
                android:id="@+id/loadingIndicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_medium"
                android:visibility="gone"
                app:layout_constraintTop_toBottomOf="@id/inviaButton"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.core.widget.NestedScrollView>
</layout>