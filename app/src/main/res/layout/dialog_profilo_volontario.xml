<?xml version="1.0" encoding="utf-8"?>
<!-- finestra modale di sola lettura: header prugna (avatar+nome), valutazione, bio, recensioni -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_dialog_card"
    android:clipToOutline="true">

    <!-- header prugna: avatar + "Profilo volontario" + nome + chiusura -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/care_primary"
        android:padding="@dimen/spacing_medium">

        <ImageView
            android:id="@+id/profiloAvatar"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_avatar_circle"
            android:src="@drawable/ic_person"
            android:padding="12dp"
            app:tint="@color/care_primary"
            android:contentDescription="@null" />

        <ImageButton
            android:id="@+id/closeButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentTop="true"
            android:src="@drawable/ic_annulla"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:tint="@color/care_on_primary"
            android:contentDescription="Chiudi" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/profiloAvatar"
            android:layout_toStartOf="@id/closeButton"
            android:layout_centerVertical="true"
            android:layout_marginStart="@dimen/spacing_medium"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Profilo volontario"
                android:textSize="@dimen/text_size_label"
                android:textColor="@color/care_on_primary"
                android:alpha="0.85" />

            <TextView
                android:id="@+id/profiloNomeText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_headline"
                android:textStyle="bold"
                android:textColor="@color/care_on_primary"
                tools:text="Mario Rossi" />

        </LinearLayout>

    </RelativeLayout>

    <!-- CORPO scorrevole -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/screen_padding">

            <!-- caso: nessuna valutazione -->
            <TextView
                android:id="@+id/profiloRatingText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_body"
                android:textColor="@color/care_on_surface_variant"
                android:visibility="gone"
                tools:text="Non ancora valutato" />

            <!-- caso: con valutazione -> numero grande + stelle + conteggio -->
            <LinearLayout
                android:id="@+id/ratingRow"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:visibility="gone"
                tools:visibility="visible">

                <TextView
                    android:id="@+id/profiloRatingNumero"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="40sp"
                    android:textStyle="bold"
                    android:textColor="@color/care_primary"
                    tools:text="4,9" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/spacing_medium"
                    android:orientation="vertical">

                    <RatingBar
                        android:id="@+id/profiloRatingBar"
                        style="?android:attr/ratingBarStyleIndicator"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:isIndicator="true"
                        android:numStars="5"
                        android:stepSize="0.5"
                        android:progressTint="@color/care_accent"
                        android:secondaryProgressTint="@color/care_accent"
                        android:progressBackgroundTint="@color/care_outline" />

                    <TextView
                        android:id="@+id/profiloNumeroValutazioni"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="2dp"
                        android:textSize="@dimen/text_size_label"
                        android:textColor="@color/care_on_surface_variant"
                        tools:text="su 24 valutazioni" />

                </LinearLayout>

            </LinearLayout>

            <TextView
                android:id="@+id/profiloBioText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_medium"
                android:textSize="@dimen/text_size_body"
                android:textColor="@color/care_on_surface"
                tools:text="Pensionato, disponibile per spesa e commissioni in zona centro. Volentieri due chiacchiere!" />

            <!-- titolo recensioni: visibile solo se ce n'è almeno una -->
            <TextView
                android:id="@+id/recensioniTitolo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_medium"
                android:text="Recensioni"
                style="@style/CareConnect.SectionLabel"
                android:visibility="gone"
                tools:visibility="visible" />

            <!-- contenitore riempito a runtime con un riquadro per recensione -->
            <LinearLayout
                android:id="@+id/recensioniContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />

            <!-- chiusura -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/chiudiButton"
                style="@style/CareConnect.Button.Primary"
                android:layout_width="match_parent"
                android:layout_height="@dimen/button_height"
                android:layout_marginTop="@dimen/spacing_large"
                android:text="Chiudi" />

        </LinearLayout>

    </ScrollView>

</LinearLayout>
